# Handoff snapshot — 2026-09-16

Written to let work continue on a different PC from the correct step. Not part of the standard
doc set (`ROADMAP.md`/`JOURNAL.md`/`CHANGELOG.md` remain authoritative and live) — this is a
one-time orientation snapshot. Safe to delete once the other PC is caught up.

---

## 1. Where things stand

**Repo:** `Andrei7486/rf_verification_app`. **`master`** is at `4dc7e82` (S3 merged, PR #21).

**Currently checked out here:** `docs/s-m0-bench-acceptance`, 5 commits ahead of `master`,
in sync with `origin` (nothing to push). This branch documents the bench acceptance that already
happened on real hardware and has **not been merged to `master` yet** — check GitHub for an open
PR before starting new work; `gh` wasn't available in this session to confirm PR status directly.

Commits on this branch, not yet on `master`:
```
30c3e05 docs(roadmap): S-M0 bench-accepted, S-M0-V closed
b03ff8a docs(tracker): drop done-unverified status
c5329c3 docs(journal): S-M0 bench-accepted
d1a129d docs(spec): M7 acceptance status - bench-accepted
3541bc2 docs(changelog): v0.7.0 bench-accepted
```

**Tag:** `v0.7.0` exists, on `master`, pointing at the S-M0 closure merge commit (`3e88ba1`).

### Stage status (per `docs/ROADMAP.md`'s table, as of this branch)

| Stage | What | Status |
|---|---|---|
| S0 | Doc/repo scaffolding | in-progress |
| S-M0 | Power Accuracy per-point overhead | **done**, `v0.7.0`, accepted 2026-09-08 |
| S-M0-V | S-M0 bench acceptance (criteria 1-8) | **done**, accepted 2026-09-08 |
| S1 | Config integrity & drift detection (M6) | merged to `master`, roadmap table still says `in-review` — no tag |
| S2 | Immediate response after Start Run (U2) | merged to `master`, roadmap table still says `in-review` — no tag |
| S3 | Real-time logs / SSE (U3) | merged to `master`, roadmap table still says `in-review` — no tag |
| S4 | Scrollable results area (U4) | planned, not started |

**S-M0 bench acceptance, what's actually covered:** criteria 1, 3, 4, 5, 6 satisfied by the
2026-09-08 hardware run (manual verification against the CXA front panel, shipped defaults:
`use_prompt_read=true`, `enable_adc_power_check=false`, `dut_settle_after_power_s=0.5`). Criterion
7 (0.5 s vs 1.5 s settle-time distinguishing test) was **not** run as a distinguishing test — 0.5 s
is only shown *sufficient* by outcome, not compared against the alternative. Criterion 8 (Flatness
regression) was not run but Flatness is unaffected by construction (`flatness.use_prompt_read`
defaults `false`). This is recorded, not a gap to silently close.

**S1/S2/S3 open item:** these three are merged to `master` and working, but per
`DEVELOPMENT_RULES.md`'s Definition of Done they aren't `done` until bench-accepted and tagged.
None of the three needed hardware to implement (all R0/pure-UI or pure-config-diff logic), but
none has had its Release Smoke Test run yet either. That's the next process debt to close, likely
before starting S4.

**P0 gates (blocking S9 onward, from `docs/ROADMAP.md`):** calibration JAR, `:CORR:SA:GAIN`
front-panel read, `pwr_calibration.csv` presence check, a short post-calibration baseline run, and
supplying the legacy IQ `.class` files (unblocks the whole M-track). Status of these gates wasn't
re-confirmed this session — check `docs/ROADMAP.md` §P0 and the operator directly.

---

## 2. Uncommitted / local-only state on THIS PC — will not transfer automatically

These are on this machine's working tree only. If the other PC needs any of them, copy explicitly
— `git pull` alone won't bring them over.

- **`config/config.json` — modified, uncommitted.** Working copy has drifted from the committed
  values (`flat_power_dbm`, `dut_com_port`, `dut_prompt_wait_timeout_s`, `if_atten_db`,
  `lband_atten_db`, `pwr_step_db`, `dut_telnet_port` all differ). This looks like real bench-session
  state (COM5 vs COM16, calibration attenuation values) rather than a mistake — **do not blindly
  discard it**, but also don't assume it's the right config for a different station. This is
  exactly the kind of drift S1's `diff_against_defaults()` now surfaces at run start.
- **`SM0_AB_TEST_RESULTS.md`** (repo root, untracked) — draft of the 2026-08-27 serial A/B test.
  Superseded: this content is already folded into `docs/JOURNAL.md`'s 2026-08-27 entry and
  `docs/bench/`. Safe to delete once confirmed redundant.
- **`config/freq_lists/*.txt`** (untracked) — bench frequency lists (`NS330_IQ.txt`,
  `NS330_Power Cal_ freqs.txt`, `freqs.txt`, `freqs_iq.txt`). Not referenced anywhere in the
  committed doc set found this session — likely local working inputs for manual bench runs, not
  app-consumed config. Confirm before assuming they're needed elsewhere.
- **`python/`** (untracked, ~41 MB) — an embeddable/portable Python distribution sitting in the
  repo root, plus **`get-pip.py`** — a pip bootstrap script. Consistent with prepping an offline
  install for the Python 3.8 win32 target (`CLAUDE.md`'s hard constraint), but not part of the
  tracked repo and not `.gitignore`d either. Worth a decision: gitignore it, move it outside the
  repo, or intentionally commit it if the offline-install story requires it in-repo.
- **`.idea/`** (untracked) — JetBrains IDE project files, not gitignored. Cosmetic; ignore or add
  to `.gitignore` at your convenience.

---

## 3. Reading order for a fresh session on the other PC

Per `CLAUDE.md`: `docs/DEVELOPMENT_RULES.md` → `docs/VALIDATION_APP_SPEC.md` → `docs/ROADMAP.md` →
`docs/JOURNAL.md` (read the last ~5 entries, newest at bottom) → `POWER_ACCURACY_HANDOFF.md` →
relevant `docs/adr/*`. `docs/JOURNAL.md`'s most recent entry is **"S-M0 bench-accepted"**
(2026-09-08) — that's the true "you are here" marker; the branch you're reading it from
(`docs/s-m0-bench-acceptance`) is what records it.

## 4. Suggested next steps

1. Confirm whether `docs/s-m0-bench-acceptance` has an open PR; merge it to `master` if reviewed
   (this closes out the S-M0 bench-acceptance documentation).
2. Decide whether S1/S2/S3 get a combined or individual Release Smoke Test + tag, since none is
   formally `done` yet despite being live on `master`.
3. Triage the uncommitted `config/config.json` drift and the untracked files above before doing
   anything that touches them.
4. Otherwise: next planned stage is **S4** (scrollable results area, U4, R0, weight 1) — not
   started.
